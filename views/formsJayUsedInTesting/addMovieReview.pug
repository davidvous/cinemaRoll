doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
    h1=`${csrfToken}`
        form(type="submit" method="post" action=`http://localhost:8080/movies/${movieId}/reviews`)
            input(type='hidden' name='_csrf' value=csrfToken)
            input(type="text", name="title" placeholder="Enter the title of your review.") 
            textarea(name="reviewText", cols="30", rows="10", placeholder='Write your review here') 
            button(type='submit') Submit your review!
 
