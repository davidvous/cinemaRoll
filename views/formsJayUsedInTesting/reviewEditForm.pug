doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
    h1= `${reviewToEdit.reviewText}`
    form(type="submit" method="post" action=`http://localhost:8080/movies/${movieId}/reviews/${reviewId}/delete`) 
        button(type='submit') Delete this review!
